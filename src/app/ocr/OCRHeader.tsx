export default function OCRHeader() {
    return (
      <section className="ocr-header">
        {/* styles applied in page via module class */}
        <h1>Try Our OCR</h1>
        <p>Upload an image and get Khmer text instantly</p>
      </section>
    );
  }
  